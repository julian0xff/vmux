# VmuxSession

Session persistence. Codable snapshot types for saving and restoring window layouts.

- `SessionPersistence.swift` — defines SessionSnapshotSchema, persistence limits, and all Session*Snapshot structs
- Depends on VmuxCore + Bonsplit
