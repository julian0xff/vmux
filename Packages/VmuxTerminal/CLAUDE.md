# VmuxTerminal

Terminal engine bridge. Provides TerminalEngine singleton accessor so packages can reach Ghostty via TerminalEngineProtocol without importing GhosttyKit directly.

- `VmuxTerminal.swift` — single file, re-exports VmuxCore
