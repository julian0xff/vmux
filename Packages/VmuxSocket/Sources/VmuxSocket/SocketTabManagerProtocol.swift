import Foundation
import VmuxCore

/// Protocol interface for socket commands to access TabManager.
/// Requirements will be filled in Phase 3b via compiler-guided extraction.
@MainActor
public protocol SocketTabManagerProtocol: AnyObject {
    // TODO: Phase 3b — change TerminalController.tabManager type to this protocol,
    // then add every property/method the compiler complains about.
}
