# VmuxSocket

Socket IPC protocol layer. Defines abstract interfaces for socket commands to access the app.

- `SocketTabManagerProtocol.swift` — 22-method protocol for workspace/tab operations
- `SocketControlSettings.swift` — socket mode, path, and password settings
- Depends on VmuxCore
