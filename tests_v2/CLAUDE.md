# tests_v2

Python socket API test suite (80+ tests). Connects to a running vmux instance's Unix socket to test V2 commands. Never run locally — CI only (see root CLAUDE.md testing policy). Set VMUX_SOCKET env var to target a specific socket.
