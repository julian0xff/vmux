interface:
  display_name: "vmux Release"
  short_description: "Prepare and publish vmux releases from version bump to tag."
  default_prompt: "Use this skill to run the vmux release workflow: choose version, generate user-facing changelog updates, bump versions, open and monitor release PRs, merge, tag, and verify release artifacts."
