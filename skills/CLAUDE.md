# skills

Claude Code skill definitions for vmux development. Each subdirectory (release, vmux, vmux-browser, vmux-debug-windows, vmux-markdown) contains SKILL.md files that define agent shortcuts and prompts for common development tasks.
