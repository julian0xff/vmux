# vmuxTests

Swift unit tests (XCTest, 14 files). Built by `vmux-unit` scheme. Tests shortcut routing, session persistence, config parsing, keyboard input, command palette search, and socket password store. Safe to run locally: `xcodebuild -scheme vmux-unit test`.
