# CLI

Contains vmux.swift, the CLI companion tool. Socket client that connects to the running vmux app's Unix socket to execute workspace, pane, and surface commands. Built as the `vmux-cli` Xcode target.
