# tests

Legacy test suite (shell + Python). Used by CI workflows (ci.yml, nightly.yml, release.yml) for guard scripts and integration tests. Partially superseded by tests_v2/ but still active in CI.
