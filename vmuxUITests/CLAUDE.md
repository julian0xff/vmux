# vmuxUITests

Swift UI automation tests (XCUITest, 13 files). Tests sidebar resize, workspace close dialogs, menu key routing, browser navigation, update pill visibility. Triggered via `gh workflow run test-e2e.yml`. Never run locally.
