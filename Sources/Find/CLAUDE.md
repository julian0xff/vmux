# Find

In-content search overlays.

- `SurfaceSearchOverlay.swift` — terminal find-in-page overlay (mounts from AppKit portal layer, not SwiftUI panel containers)
- `BrowserSearchOverlay.swift` — find overlay for WKWebView
- `BrowserFindJavaScript.swift` — JS injection for web page text search
