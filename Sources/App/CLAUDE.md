# App

App lifecycle and entry points.

- `vmuxApp.swift` — @main struct owning all @StateObject instances and settings UI
- `AppDelegate.swift` — NSApplicationDelegate, keyboard event monitoring, multi-window management, session persistence, menu construction
- `AppleScriptSupport.swift` — scripting bridge (.sdef)
