# Utilities

Small system-level helpers.

- `KeyboardLayout.swift` — translates physical key codes to characters via TIS API
- `PortScanner.swift` — scans localhost ports via batched lsof/ps
- `UITestRecorder.swift` — writes JSON counters for UI test assertions (DEBUG only)
