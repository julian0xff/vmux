# Notifications

Terminal notification system.

- `TerminalNotificationStore.swift` — central ObservableObject tracking bell/notification events per surface, managing unread counts, dock badge, and sound playback
- `NotificationsPage.swift` — SwiftUI sidebar notifications list UI
