# Resources

App bundle resources. Info.plist (bundle configuration, UTType declarations). Localizable.xcstrings + InfoPlist.xcstrings (localized strings, English + Japanese). vmux.sdef (AppleScript dictionary). bin/ghostty (bundled Ghostty binary). shell-integration/ and terminfo-overlay/ (terminal environment setup).
